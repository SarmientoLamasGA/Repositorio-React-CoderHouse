import react from "react";
import { useCart } from "../../contexts/CartContext";

function Cart() {
  return (
    <section className="CartSection">
      <div className></div>
    </section>
  );
}

export default Cart;
